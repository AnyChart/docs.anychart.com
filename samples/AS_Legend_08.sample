{:tags []
     :is_new false
     :exports "chart"}

anychart.onDocumentReady(function() {
  
  //set chart type
  var chart3 = anychart.column();
  
  // set data
  chart3.column([
    {x: 'John', value: 10000},
    {x: 'Jake', value: 12000},
    {x: 'Peter', value: 16000}
  ]);
  
  // set chart title
  chart3.title().text('Legend for Single Series');
  
  // legend settings
  chart3.legend()
    .enabled(true)                  // enable legend
    .itemsSourceMode('categories'); // set source of legend items
  
  // set container and draw chart
  chart3.container('container').draw();
});