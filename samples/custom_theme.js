var customTheme = {
  // define settings for bsr charts
  "bar": {
    // set chart title
    "title": "Bar Chart",
    // settings for default x axis
    "defaultXAxisSettings": {
      // set x axis title
      "title": "Retail Channel"
    },
    // settings for x scale
    "xScale":{
      // create list of names to show on x axis
      "names": [
        "Department Stores",
        "Discount Stores",
        "Men's/Women's Specialty Stores",
        "All other outlets"
      ]
    },
    // settings for default y axis
    "defaultYAxisSettings": {
      // set axis name
      "title": 'Sales'
    }
  }
};